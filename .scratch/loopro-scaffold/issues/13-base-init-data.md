# 13: `BaseInitData`

**What to build:** 개발자가 앱을 처음 띄우면 회원 5명·글 5개·댓글 5개가 이미 들어 있다. 빈 화면부터 시작하지 않아도 되고, 권한 분기와 공개 범위와 마크다운 렌더링을 바로 눈으로 확인할 수 있다. 재시작해도 데이터가 불어나지 않는다.

**Blocked by:** 09 (`Comment`), 10 (`PostLike`)

**Status:** done

- [x] 애플리케이션 시작 시 실행되는 러너가 샘플 데이터를 만든다. 트랜잭션 안에서 동작한다
- [x] `Member`가 한 명이라도 있으면 아무것도 하지 않고 종료한다
- [x] `Member` 5명, `Post` 5개, `Comment` 5개를 만든다
- [x] `Member` 중 한 명은 `ADMIN`이다. 나머지는 `USER`다
- [x] `Post` 중 최소 하나는 발행되지 않은 상태다. 공개 범위 동작을 눈으로 확인하기 위함이다
- [x] 샘플 본문에 마크다운 제목·목록·코드블록이 들어 있다. 렌더링을 확인하기 위함이다
- [x] 개발 모드에서 앱을 껐다 켜도 데이터가 불어나지 않는다
- [x] 테스트 모드에는 영향을 주지 않는다
